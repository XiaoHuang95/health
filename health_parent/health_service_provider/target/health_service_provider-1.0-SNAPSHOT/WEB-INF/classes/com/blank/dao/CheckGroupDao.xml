<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blank.dao.CheckGroupDao">
    <!--添加检查项-->
    <insert id="add" parameterType="com.blank.pojo.CheckGroup">
        <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into t_checkgroup(code,name,helpCode,sex,remark,attention)
        values (#{code},#{anme},#{helpCode},#{sex},#{remark},#{attention})
    </insert>
    <!--设置检查组和检查项的关系-->
    <insert id="setCheckGroupAndCheckItem" parameterType="hashmap">
        insert into t_checkgroup_checkitem(checkgroup_id,checkitem_id)
        values (#{checkgroup_id},#{checkitem_id})
    </insert>
    <!--条件查询-->
    <select id="selectByCondition" parameterType="String" resultType="com.blank.pojo.CheckGroup">
        select * from t_checkgroup
        <if test="value !=null and value.length > 0">
            where code like concat("%",#{code},"%") or name like concat("%",#{name},"%")
            or helpCode like concat("%",#{helpCode},"%")
        </if>
    </select>
    <!--根据id查询-->
    <select id="findById" resultType="com.blank.pojo.CheckGroup" parameterType="java.lang.Integer">
        select * from t_checkgroup where id = #{id}
    </select>
    <!--根据检查组id查询与对应的检查项信息-->
    <select id="findCheckItemIdsByCheckGroupId" parameterType="int" resultType="int">
        select checkitem_id from t_checkgroup_checkitem where checkgroup_id = #{checkGroupId}
    </select>
    <!--根据检查组id删除中间表数据（清理原有关联关系）-->
    <delete id="deleteAssociation" parameterType="int">
        delete from t_checkgroup_checkitem where checkgroup_id = #{id}
    </delete>
    <!--编辑检查组信息-->
    <update id="edit" parameterType="com.blank.pojo.CheckGroup">
        update t_checkgroup
        <set>
            <if test="code != null">
                code = #{code},
            </if>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="helpCode != null">
                helpCode = #{helpCode},
            </if>
            <if test="sex != null">
                sex = #{sex},
            </if>
            <if test="remark != null">
                remark = #{remark},
            </if>
            <if test="remark != null">
                attention = #{attention}
            </if>
        </set>
        where id = #{id}
    </update>
    <!--删除检查组-->
    <delete id="deleteById" parameterType="int">
        delete from t_checkgroup where id = #{id}
    </delete>
</mapper>